server:
  port: 8084
  forward-headers-strategy: framework

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true

spring:
  application:
    name: gateway

services:
  deal:
    url:
  statement:
    url: