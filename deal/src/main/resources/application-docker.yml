server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    username: ilya
    password: ${DB_PASSWORD}
  kafka:
    bootstrap-servers: kafka:9092

services:
  calculator:
    url: http://calculator:8080/calculator